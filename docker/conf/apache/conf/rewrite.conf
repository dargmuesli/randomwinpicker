<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule .* https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
    RewriteCond %{HTTP_HOST} ^www\.randomwinpicker\.de$ [NC]
    RewriteRule ^(.*)$ https://randomwinpicker.de/$1 [L,R=301]
</IfModule>
